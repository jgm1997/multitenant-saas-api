{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "saas-api.serviceAccountName" . }}
  labels:
    {{- include "saas-api.labels" . | nindent 4 }}
{{- end }}
